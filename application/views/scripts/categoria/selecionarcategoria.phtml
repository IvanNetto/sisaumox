<?php ?>
<form class="form-horizontal" action="<?= $this->baseUrl() . '/produtosolicitacao/buscarprodutosporcategoria'?>" method="POST">
    <fieldset>
        <legend>Selecinando uma categoria</legend>
        <input type="hidden" name="solicitacaoid" value="<?php echo $this->idsolicitacao;?>">
        <div class="form-group">
            <label for="select" class="col-lg-2 control-label">Categoria</label>
            <div class="col-lg-10">
                <select class="form-control" id="select" name="categoriaid">
                    <option></option>
                    <?php foreach ($this->listadecategorias as $nomeCategoria): ?>
                        <option value="<?php echo $nomeCategoria['id']; ?>"><?php echo $nomeCategoria['nome']; ?></option>
                    <?php
                    endforeach;
                    ?>
                </select>
                <br>
            </div>
        </div>
        <div class="form-group">
            <div class="col-lg-10 col-lg-offset-2">
                <!-- <button class="btn btn-default">Cancelar</button>-->
                <a href="<?php echo $this->url(array('controller' => 'solicitacao', 'action' => 'listarsolicitacao')); ?>">Voltar</a>
                <button type="submit" class="btn btn-primary" id="">Listar Produtos</button>
            </div>
        </div>
    </fieldset>
</form>