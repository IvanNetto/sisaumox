<h3>Lista de solicitações concretizadas</h3>
<hr>
<?php if ($this->historico->current() <> null) { ?>
<p> Abaixo seguem suas solicitações finalizadas. </p>
<?php } else { ?>
    <p> Você não tem ainda solicitações concretizadas, porisso seu histórico encontra-se vazio.</a> </p>
<?php } ?>

<div class="row">
    <div class="panel panel-primary filterable">
        <div class="panel-heading">
            <span class="glyphicon glyphicon-list"></span>Histórico
        </div>
        <table class="table">
            <tr>
                <?php if ($this->historico->current() == null) { ?>
                <center><strong><h4>sem solicitações concretizadas</h4></strong></center>
                </tr>
            <?php } else { ?>
                <thead>
                    <tr class="filters">
                        <th><input type="text" class="form-control" placeholder="Código" disabled></th>
                        <th><input type="text" class="form-control" placeholder="Data" disabled></th>
                        <th><input type="text" class="form-control" placeholder="Status" disabled></th>  
                        <th class="text-center">Ação</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <?php foreach ($this->historico as $solicitacao) { ?>
                            <td><?= $solicitacao['id']; ?></td>
                            <td><?= $solicitacao['data']; ?></td>
                            <td><?= $solicitacao['status']; ?></td>
                            <td class="text-center"><a class='btn btn-info btn-xs' href="<?php echo $this->url(array('controller' => 'produtosolicitacao', 'action' => 'resumodesolicitacao', 'solicitacaoid' => $solicitacao['id'])); ?>"><span class="glyphicon glyphicon-edit"></span>Resumo</a> 
                        </tr> 
                    <?php }
                }
                ?>
                </tr>
            </tbody>
        </table>
    </div>     
</div>


