<h3>Lista de solicitações agendadas</h3>
<hr>
<?php if ($this->agendadas->current() <> null) { ?>
<p> Abaixo seguem suas solicitações agendadas. </p>
<?php } else { ?>
    <p> Não consta nenhma solicitação agendada até o momento na base de dados.</a> </p>
<?php } ?>

<div class="row">
    <div class="panel panel-primary filterable">
        <div class="panel-heading">
            <span class="glyphicon glyphicon-list"></span>Solicitações Agendadas
        </div>
        <table class="table">
            <tr>
                <?php if ($this->agendadas->current() == null) { ?>
                <center><strong><h4>sem solicitações agendadas</h4></strong></center>
                </tr>
            <?php } else { ?>
                <thead>
                    <tr class="filters">
                        <th><input type="text" class="form-control" placeholder="Código" disabled></th>
                        <th><input type="text" class="form-control" placeholder="Data" disabled></th>
                         
                        <th class="text-center">Ação</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <?php foreach ($this->agendadas as $solicitacao) { ?>
                            <td><?= $solicitacao['id']; ?></td>
                            <td><?= $solicitacao['data_agendamento']; ?></td>
                            
                            <td class="text-center"><a class='btn btn-info btn-xs' href="<?php echo $this->url(array('controller' => 'produtosolicitacao', 'action' => 'carrinhodesolicitacoes', 'solicitacaoid' => $solicitacao['id'])); ?>"><span class="glyphicon glyphicon-edit"></span>Visualizar</a>

                        </tr> 
                    <?php }
                }
                ?>
                </tr>
            </tbody>
        </table>
    </div>     
</div>


